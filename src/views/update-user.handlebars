<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Actualizar usuario</title>
  <style>
    body {
      margin: 0;
      background-color: #121212;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      color: #e0e0e0;
    }
    main {
      max-width: 420px;
      margin: 60px auto;
      padding: 40px 30px;
      background-color: #1e1e1e;
      border-radius: 12px;
      box-shadow: 0 0 20px rgba(0,0,0,0.5);
    }
    label {
      display: flex;
      flex-direction: column;
      font-weight: 500;
      margin-bottom: 16px;
    }
    input {
      padding: 10px;
      border: 1px solid #333;
      border-radius: 8px;
      background-color: #2a2a2a;
      color: #f0f0f0;
    }
    button {
      padding: 12px;
      background-color: #2196f3;
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      width: 100%;
    }
  </style>
</head>
<body>
  <main>
    <h2 style="text-align: center; color: #90caf9; margin-bottom: 24px;">Actualizar Usuario</h2>

    <form id="userUpdate-form">
      <label>
        Nombre:
        <input type="text" name="first_name" placeholder="Ej: Juan" required />
      </label>

      <label>
        Apellido:
        <input type="text" name="last_name" placeholder="Ej: Pérez" required />
      </label>

      <label>
        Edad:
        <input type="number" name="age" placeholder="Ej: 30" min="1" required />
      </label>

      <button type="submit">Actualizar</button>
    </form>
  </main>

  <script src="/scripts/updateUser.js"></script>
</body>
</html>
